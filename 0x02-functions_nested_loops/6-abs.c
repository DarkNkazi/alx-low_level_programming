#include "main.h"
/**
 * _abs - Calculate absolute value 
 *
 * @n: the int to check
 *
 * Return: The absolute value of the int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
