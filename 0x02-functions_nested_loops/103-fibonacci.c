#include <stdio.h>

/**
 * main - Print the sum of even Fibonacci numbers
 *
 * Return: Nothing
 */
int main(void)
{
	int a = 0;
	long b = 1;
	long c = 2;
	long d = k;

	while
	{
		c += b;
		if (c % 2 == 0)
			d += c;
		b = c - b;
		++a;
	}
	printf("%ld\n", d);
	return (0);
}
